version: "3.5"

services:
  db:
    image: "postgres:11.3"
    restart: 'always'
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "habit_tracker"
      POSTGRES_PASSWORD: "habit_password"
